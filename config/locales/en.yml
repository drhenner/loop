# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  company:                'Loop de Luxe'
  hello:                  'Hello'
  out_of_stock_notice:    'Sorry this item is out of stock.'
  item_saved_for_later:   'The item will be saved for later.'
  reset_password_title:   'Reset Password'
  reset_password_text:    'Enter your new password.'
  loop_phone_number:      '(555) 123-4567'
  customer_service_hours: '11am-6pm, Monday to Sunday ET'
  minimum_wait_minutes:   5


  date:
    formats:
      # Use the strftime parameters for formats.
      # When no format has been given, it uses default.
      # You can provide other formats here if you like!
      default: "%m/%d/%Y"
      short: "%b %d"
      long: "%B %d, %Y"
      us_date: "%m/%d/%Y"
  time:
    formats:
      us_date: "%m/%d/%Y"
      us_time: "%m/%d/%Y @ %I:%M %p"
      file_us_time: "%m-%d-%Y_%I-%M-%p"
      long_date: "%B %d, %Y"

  helpers:
    submit:
      myaccount:
        user:
          update: "Change"
