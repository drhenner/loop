<h1> Edit Variant </h1>

<%= form_for(@variant, :url => admin_merchandise_images_variant_path(@variant),
                        :html => {:class => 'prepend-4', :multipart => true}) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <div class="button generic_submit_button">
    <%= submit_tag "Create",
                    :class  => "button",
                    :id     => "create_variant_button" %>
  </div>
<% end %>

  <%= link_to 'Show', admin_merchandise_images_variant_path( @variant ), :class => 'button' %> |
