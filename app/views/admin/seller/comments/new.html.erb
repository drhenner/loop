<%= render :partial => '/admin/seller/sub_header'%>
<h1> New Comment </h1>

<%= form_for(@comment, :url => admin_seller_ticket_comments_path(@ticket,@comment)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

  <%= link_to 'Back to Ticket', admin_seller_ticket_url(@ticket), :class => 'button white-button medium-button' %>
