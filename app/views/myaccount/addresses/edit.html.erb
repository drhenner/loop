<h1> Edit Address </h1>

<%= form_for(@address, :url => myaccount_address_path(@address)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

  <%= link_to 'Back', myaccount_addresses_path, :class => 'b-link' %>
