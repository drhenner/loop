<h1> New Measurement" </h1>

<%= form_for(@measurement, :url => myaccount_measurements_path(@measurement)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

  <%= link_to 'Back to Measurements', myaccount_measurements_path, :class => 'button' %>
