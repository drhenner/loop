<h1> New Measurements </h1>

<%= form_for(@measurement, :url => myaccount_measurements_path(@measurement)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

  <%= link_to 'Back', myaccount_measurements_path, :class => 'b-link' %>
