<h1> Edit Measurements </h1>

<%= form_for(@measurement, :url => myaccount_measurement_path(@measurement)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

  <%= link_to 'View Measurements', myaccount_measurements_path, :class => 'button' %>
