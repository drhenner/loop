<% if @measurement.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@measurement.errors.count, "error") %> prohibited this measurement from being saved:</h2>

    <ul>
    <% @measurement.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

  <div class="field">
    <%= f.label :shirt_size %><br />
    <%= f.select :shirt_size, Measurement::SHIRT_SIZES, :include_blank => true %>
  </div>

  <div class="field">
    <%= f.label :dress_size %><br />
    <%= f.select :dress_size, Measurement::DRESS_SIZES, :include_blank => true %>
  </div>

  <div class="field">
    <%= f.label :waist %><br />
    <%= f.select :waist, Measurement::WAIST_SIZES, :include_blank => true %>
  </div>

  <div class="field">
    <%= f.label :pant_length %><br />
    <%= f.select :pant_length, Measurement::PANT_LENGTHS, :include_blank => true %>
  </div>

  <div class="field">
    <%= f.label :shoe_size %><br />
    <%= f.select :shoe_size, Measurement::SHOE_SIZES, :include_blank => true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
